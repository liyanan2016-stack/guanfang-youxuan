# 更新日志

版本号规则：`versionName` = 这里的版本号，Go 核心层 `libVersion` 与之同步，
`versionCode` 单调递增（v1.21 = 22）。签名指纹 `c2:d4:0f:52…` 自 v1.2 起未变，
所有版本可直接覆盖安装，历史记录和设置保留。

---

## v1.21

**版本号露出界面。** 顶栏标题右侧显示 `v1.21`，点一下弹「关于」，里面写了应用
版本、`versionCode` 和核心库版本。之前只能去系统的应用信息里翻——用户报问题时
第一句话总是「我用的哪个版本」，这个信息不该藏起来。

核心库版本单独列出来是因为 Android 外壳和 Go 核心是两套独立版本号，理论上可能
不一致（比如只重打了包没重编 `.aar`），出问题时要能分别确认。

**使用说明和更新日志进 App。** 关于弹窗下方两个按钮。使用说明只放 7 条「第一次
用会卡住的地方」，按实际提问频率排；完整教程仍在仓库的
[使用教程.md](使用教程.md)。更新日志留最近几版，完整历史在本文件。

**补上鸣谢。** 见 [README 的鸣谢](README.md#鸣谢)。

**文档精简。** 使用教程从 428 行压到 143 行，README 从 454 行压到 186 行。原来把
「怎么用」和「为什么这么实现」混在一起，找一句话要划半天。现在使用教程只讲操作，
README 只讲设计取舍，版本沿革全部收进本文件。

---

## v1.20

**指定 IP 段优选。** 高级选项里新增一个多行输入框，填了就只扫你给的段，不读也
不下载官方列表。候选池从 6500 多个 `/24` 降到你指定的规模，扫描从几分钟降到几十
秒，命中率也高得多。

- 支持 CIDR（`104.16.0.0/13`）、单 IP、v4 起止范围（`104.16.0.0-104.16.3.255`）、
  IPv6 CIDR；一行一条或逗号/空格分隔，`#` 与 `//` 注释会被忽略
- 大段自动切成 `/24`（IPv6 切 `/48`），上限 20000 个，超了截断并告知
- 边打边校验，实时显示「已识别 N 个子网」或具体哪一条填错
- 填了 v4 段却在扫 IPv6 会直接提示切换协议，不会让人对着空结果猜
- 指定段时测完全部子网才收手（官方列表模式下 30% 覆盖率就够），因为你给的池子
  本来就是精挑的
- 结果里显示「数据源」，标明这次用的是指定段还是官方列表

顺带修了两个原有 bug：`getRandomIPv4s` 按固定 8 位随机，遇到 `/28` 这种会拼出段
外地址；重试次数固定 4 次，`/31` 这种小段可能一个 IP 都产不出。现在按前缀长度算
地址空间。

---

## v1.19

**测速源可选。** 「优选出来很快、实际用起来慢」的头号原因是速度数字来自别人的
域名，而运营商对不同域名的限速策略差别很大。现在五档可选：

| 选项 | 地址 |
| --- | --- |
| 自动选择（默认） | 探测运营商后自动挑，移动线路换用对移动友好的源 |
| Cloudflare | `speed.cloudflare.com/__down?bytes=99999999` |
| CM提供 | `cf.090227.xyz/__down?bytes=99999999` |
| 移动专属 | `speed.okl.abrdns.com` |
| 手动输入 | 你自己域名下的大文件，最准 |

自动档通过 `cf.json` 读 `asn` / `asOrganization` 判断是否中国移动，这个请求禁用
代理（`Proxy: nil`），否则探到的是代理出口的运营商。

**速率限制熔断。** 公共测速端点短时间反复扫会撞 HTTP 429。以前 429 会被当成
「这个 IP 慢」，于是整轮结果全被压低还找不出原因。现在识别 429 并提示「触发速率
限制」，提前结束测速。

---

## v1.18

**三项收进「高级选项」折叠区。** 节点域名（SNI）、自定义测速地址、测速时长从
主界面移入折叠区，收起时标题右侧显示摘要（如「已填域名 · 自定义测速 · 测速 10
秒」）。这三项绝大多数人不动，但一旦动过就必须能一眼看出动了什么——所以折叠归
折叠，摘要必须留。

---

## v1.17

**测速地址可自定义，且真正生效。** v1.16 之前测速地址硬编码，而且有个隐蔽问题：
即使填了自定义 SNI，实际连接用的 `TLSClientConfig` 和请求 URL 仍取内置测速域名，
所以速度数字是别人域名的，不含回源你节点那一段。现在自定义地址统一作用于请求
URL、TLS SNI 和 RTT 阶段的默认 SNI。

填错不回落默认地址——直接停下报错。悄悄用默认地址等于给出一个看着正常实际无关的
数字，比报错更坏。

**测速时长可选 5 / 10 / 15 秒。** 运营商国际出口对新连接常有「突发不限速」窗口，
前几秒飞快、之后掉一个数量级。5 秒测速整段都落在窗口里，测出来比能持续用到的
带宽高得多。

**结果回显测速配置。** 新增测速地址、测速时长、检测到的运营商三个字段，外加诊断
提示（例如全部候选返回同一个非 200 状态码时会说明）。速度数字来源不再是黑盒。

---

## v1.16

**修「选地区后长时间停在第一轮」。** 这一条既是观感也是实质。

实质 bug：`regionSampler` 原本要攒满 `sampleSize=100` 个命中子网才交货，而窄地区
的命中率不到 1%，意味着要探完 6500 个子网才开始跑 RTT——期间进度条上一个结果
都没有，看着像卡死。现在已有命中时最多探 3 个批次（600 个子网）就提前交货，
无命中时该配额不生效。

观感修复：进度全程附「已用 X 秒」，换批提示带「第 N 轮·第 M 批（累计侦察
X/6500）」，侦察提示补命中率，RTT 刷新粒度从 10 个改成 5 个。慢是机制决定的，
但至少要看得出在动。

---

## v1.15

**分段控件改滑动指示块。** IP 协议、输出数量原来是 `RadioGroup`，选中反馈只能是
背景 selector 瞬间替换：高亮块「消失在这里、出现在那里」，中间没有过程，动效时
长怎么调都还是「啪」一下。改成自绘的 `SegmentedBar`，指示块滑动过去（340ms，略微
过冲再回落），位置和宽度都插值（1/5/10 三格宽度不等，只插位置会在宽度突变时闪
一下），文字颜色按指示块的实时距离插值。

顺带消掉一类 bug：`RadioGroup` 靠 view id 追踪选中项，动态创建的子项忘了设 id 就
会两个同时高亮（v1.13 的「输出数量」正是这么坏的）。现在选中项是一个 `int` 下标。

**惯性滑到边界改为反弹。** `SpringScrollView` 原来只接手手指拖动，惯性滑动撞边直
接交给父类，滚动位置走到 0 就硬生生停住。现在按剩余速度先冲出去形变再弹回来，
深度上限压在 `maxPull / 3`（惯性撞边幅度应明显小于手动拉拽），低于 800 px/s 完全
不弹（慢慢滑到底本来就不该有撞击感），手指按下立即中断反弹。

---

## v1.14

修「输出数量」1 和 5 同时高亮：动态创建的 `RadioButton` 没设 id，默认是
`View.NO_ID(-1)`，`RadioGroup` 因此跳过了取消上一个的步骤。

---

## v1.13

**输出数量可选 1 / 5 / 10。** 优选出的 IP 过一会儿可能就不好用了，一次多拿几个
省得重扫。多出来的排在结果下方「备选地址」里，整行点一下复制。

备选只显示地址 + 速度 + 延迟——机房和用时对「换下一个」这个决策没帮助，全列出来
反而要划很久。备选不入历史库，历史只存最优那一条。

要多个结果时会停用「远超目标就提前收工」的短路逻辑，改判「合格数量够了没」。

---

## v1.11 – v1.12

**液态玻璃界面。** 背景铺四团径向渐变光斑，卡片改半透明填充 + 1dp 亮描边靠它透
色（API 31+ 走 `RenderEffect` 60dp 模糊）。浅色主题首版翻车过一次——白得发灰，
配方改成 78%→56% 白 + 暗色 hairline 才立住。

**端口与地区改折叠，带摘要。** 展开要占掉大半屏，但默认值以外的设置必须能一眼
看见，所以收起时标题右侧显示「443 · 香港/日本」这样的摘要。

**补一套动效。** 新增 `Anim.java`：统一 `PathInterpolator(0.2,0,0,1)`、卡片
staggerIn、折叠区高度动画靠 tag 防抖（两个 `ValueAnimator` 同时改一个 View 的
height 会让高度永久残缺）。

---

## v1.10

**页面从三页收敛为两页。** v1.9 拆成「扫描 / 配置 / 历史」，结果参数和扫描按钮
分了家——调个带宽要切页、回来再点扫描。现在参数回归首页，排在按钮和结果下面：
跑第二轮通常只想再点一次扫描，不该每次先划过一屏参数。

emoji 全部换成矢量图标（6 个 vector drawable + selector），对齐桌面端的纯文字
风格。

---

## v1.8 – v1.9

v1.8 参数区的横向滚动改成 `FlowLayout` 自动换行；v1.9 加顶栏与 58dp 底部导航。

---

## v1.7

**回源校验。** 填了节点域名时检查回源状态码，拦掉 52x 回源不通的 IP。

根因是这样的：`testRTT` 只判断响应里有没有 `CF-RAY` 头，而 CDN 回源失败的
521–526 同样带这个头。于是「摸到了门」被当成「门后的隧道通」，用户侧表现为连上
就断（`io: read/write on closed pipe`）。

现在用白名单判定：放行 101 / 2xx / 3xx / 400 / 404 / 405 / 410，判死 403 /
521–526 / 530 / 其他 5xx。分界线是「这个响应由谁生成」——404 由源站生成，证明回源
通（隧道路径本来就该 404）；52x 由边缘自己生成，证明回源断。用白名单而不是黑名单
是因为上游未来新增错误码时，白名单最坏是少给一个候选，黑名单最坏是又放行一个坏的。

---

## v1.6

**地区侦察。** 选了地区时先探子网的落地机房，再拿命中的子网去优选。

判定是三态的：matched / mismatched / unknown。只有拿到明确否定证据（拿到了标识头
但地区不符、或明确错误码）才排除，探不通归 unknown 进候补池。随机拼 IP 的命中率
约 1/3，把「探不通」当「不符合条件」会每 3 个好子网冤枉 2 个。

---

## v1.5

一次性修掉五个影响「优选到底选了什么」的问题：

**测速形同虚设。** 默认期望带宽 1 Mbps ≈ 128 kB/s，几乎任何能连通的 IP 都达标，
于是「遇首个达标即返回」让结果实际等于「延迟最低」而不是「最快」。现在测完 top N
选最快，只在远超目标（3 倍）时才短路。

**峰值当成速度。** 原来用 `maxSpeed`，那是 TCP 慢启动后的突发峰值。改成自实现的
EWMA 平均（decay = 2/31）。

**测速不查状态码。** 4xx/5xx 错误页的字节数被当成下载内容算速度。现在只接受
200/206。

**单次 TCP 失败就判死。** 加丢包率指标：探测 4 次，丢包率超 50% 判死，排序时
丢包率优先于延迟，再看抖动。

**RTT 与测速目标域名不一致。** `testRTT` 用 `cloudflare.com` 做 SNI+Host，测速却
走另一个域名——实测有 IP 对前者返 301 通过、对后者返 403 实际不可用。现在两处对齐。

另外两个本项目独有的：`scan.go` 里 `SplitN` 缺 `else` 分支导致 URL 拼成
`https:///`；数据文件没有时效判断，下载过一次就永不更新（现在 24 小时过期，
拉取失败沿用旧副本）。

---

## v1.4

优选逻辑重写，详见 [README 的设计要点](README.md#设计要点)。

---

## v1.2

端口与地区筛选。端口这一项最要紧：Cloudflare 边缘节点不只监听 80/443，v1.1 固定
测 80/443，如果你的节点跑在 2053，工具验证的和实际使用的根本不是同一个端口——
握手能过，数据一发就被掐断。

同版本补 MIT LICENSE、数据来源声明、免责声明。

---

## v1.1

首个发布版本。基于一份早期的 Cloudflare IP 优选实现重写，修掉一批结构性问题：

**扫描永不结束。** 内外两层 `for{}` 都没有轮次上限，只要达不到期望带宽就无限
重试——带宽填成 900 Mbps 就等于永久扫描，只能靠点取消。现在有 `maxScanRounds = 10`。

**达不到目标就一无所获。** 原版只在达标时返回，没达标的结果全丢：实测 61 Mbps
本来是个可用 IP，用户什么都拿不到。现在返回历轮最佳结果并说明差距。

**测速统计漏掉最后一个窗口。** 读到 `err` 就直接 break，不满 1 秒的末尾窗口永不
结算——整个下载在 1 秒内结束时 `maxSpeed` 会是 0，明明下载成功却报速度为零。现在
先结算再判错，并用总字节数兜底算平均速度。

**取消后仍在跑。** 并发信号量满载时会阻塞，点了取消还要等已排队的 IP 测完。现在
信号量等待可被取消打断。

**取消被显示成「未找到」。** 结果里没有区分字段，界面把两者混为一谈。现在有
`cancelled` 字段。

**IPv6 前缀长度被忽略。** 原版固定保留前 48 位随机后 80 位。CF 目前公布的确实全是
`/48` 所以恰好正确，但上游一旦给出 `/64`，随机就会覆盖掉子网自身的位，拼出根本
不属于 CF 的地址。现在按实际前缀长度计算。

**每轮重复抽样。** 每轮重新洗牌取前 100 个子网，轮与轮之间没有记忆，会反复测同
一批。现在一次扫描内洗牌一次、游标推进，跨轮零重复；扫描结束即释放，不跨扫描
保留，所以换网络环境后所有子网重新平等可选。

**测速用的端口和 RTT 不一致。** 测速固定连 80/443 而 RTT 测的是用户指定的端口，
测出来的速度对应的不是实际要用的那个端口。现在两处用同一个。

**取消后更新数据静默失败。** 取消扫描会把 context 置为已取消，而 `UpdateData`
不重置它，于是下载在第一个取消检查处就返回——旧文件已删、新文件没下，进度却显示
「数据更新完成」，要到下次扫描才发现数据没了。现在 `UpdateData` 返回失败原因
（原来返回 void，界面只能无条件报成功），结束时校验文件是否真的到位，0 字节文件
（下载中断的残留）也算缺失。

**点了开始立刻点取消，取消会丢。** 核心层在任务开始时重建取消上下文，而任务跑在
后台线程里，「派发」和「真正开始」之间有一段窗口，这期间到达的取消会被重建动作
抹掉。现在多了 `BeginTask()`：界面在派发前同步调一次，把上下文先建好。

**取消提示写死了「扫描」。** 数据更新走同一个取消通道，取消更新时却提示「用户已
取消扫描」。改成中性的「正在取消...」，具体取消了什么由各任务的收尾消息覆盖。

**JDK 路径硬编码。** `android/gradle.properties` 里写死了
`org.gradle.java.home`，换一台机器直接构建失败。现在走 `JAVA_HOME`。

**夜间模式缺 danger 色。** 亮色模式的深红 `#D92D20` 放在深灰背景上对比度不足，
取消和删除按钮都看不清。夜间模式单独给了 `#FF6B5E`。

同版本补 MIT LICENSE、数据来源声明、免责声明。
