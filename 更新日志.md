# 更新日志

签名指纹 `c2:d4:0f:52…` 自 v1.2 起未变，所有版本可直接覆盖安装。

---

## v1.22

稳定性修复，无新功能。

- **扫描出错不再卡死界面。** 原来异常只画错误文字，按钮永久停在「扫描中...」且点不动，只能杀进程
- **扫描中转屏不再泄漏。** 补 `configChanges`，销毁时显式取消核心层任务（原来只中断 Java 线程，对卡在 JNI 调用里的线程无效）
- **填 IP 段不再卡顿。** 改成停手 300ms 才校验，并去掉重复展开
- **核心层崩溃不再闪退。** Go panic 会 abort 整个进程，现在三个入口都拦下来转成界面提示
- **架构不匹配会说清楚。** 拿错分包原来启动即崩，现在提示改用 universal 版本

## v1.21

- **版本号露出界面。** 顶栏标题右侧显示版本，点开「关于」有 `versionCode` 和核心库版本
- **使用说明和更新日志进 App。** 关于弹窗下方两个按钮
- **文档精简。** 使用教程 428→143 行，README 454→186 行

## v1.20

**指定 IP 段优选。** 填了就只扫你给的段，不读官方列表。候选池从 6500 多个 `/24` 降到你指定的规模，扫描从几分钟降到几十秒。

- 支持 CIDR、单 IP、v4 起止范围、IPv6 CIDR，`#` 与 `//` 注释忽略
- 大段自动切 `/24`（IPv6 切 `/48`），上限 20000 个
- 边打边校验，填了 v4 段却在扫 IPv6 会提示切协议
- 指定段时测完全部子网才收手（官方列表模式 30% 覆盖率就够）

顺带修两个 bug：`getRandomIPv4s` 按固定 8 位随机，遇 `/28` 会拼出段外地址；重试固定 4 次，`/31` 可能一个 IP 都产不出。

## v1.19

**测速源可选。**「优选快、实际慢」的头号原因是速度数字来自别人的域名，而运营商对不同域名限速差别很大。

| 选项 | 地址 |
| --- | --- |
| 自动选择（默认） | 探运营商后自动挑 |
| Cloudflare | `speed.cloudflare.com/__down` |
| CM提供 | `cf.090227.xyz/__down` |
| 移动专属 | `speed.okl.abrdns.com` |
| 手动输入 | 你自己域名下的大文件，最准 |

自动档读 `cf.json` 的 `asn` 判断是否移动，该请求禁用代理，否则探到的是代理出口。

**速率限制熔断。** 429 原来被当成「这个 IP 慢」，整轮结果被压低还找不出原因。现在识别并提前结束。

## v1.18

**三项收进「高级选项」折叠区。** 节点域名、自定义测速地址、测速时长。收起时标题右侧显示摘要 —— 折叠归折叠，动过什么必须能一眼看出。

## v1.17

**测速地址可自定义，且真正生效。** 之前即使填了 SNI，实际连接的 `TLSClientConfig` 和请求 URL 仍取内置域名，速度数字不含回源你节点那一段。填错不回落默认地址，直接报错 —— 悄悄用默认值等于给出一个看着正常实际无关的数字。

**测速时长可选 5 / 10 / 15 秒。** 运营商国际出口对新连接常有「突发不限速」窗口，5 秒整段都落在窗口里。

**结果回显测速配置。** 测速地址、时长、检测到的运营商，速度来源不再是黑盒。

## v1.16

**修「选地区后长时间停在第一轮」。** `regionSampler` 原本要攒满 100 个命中子网才交货，而窄地区命中率不到 1%，意味着要探完 6500 个子网才开始跑 RTT。现在已有命中时最多探 3 批（600 个子网）就提前交货。

进度全程附「已用 X 秒」，换批提示带轮次与累计侦察数。

## v1.15

**分段控件改滑动指示块。** 原来是 `RadioGroup`，选中只能是背景瞬间替换。改成自绘 `SegmentedBar`，指示块滑过去，位置和宽度都插值（1/5/10 三格宽度不等）。

顺带消掉一类 bug:`RadioGroup` 靠 view id 追踪选中项，动态创建的子项忘了设 id 会两个同时高亮。现在选中项是一个 `int` 下标。

**惯性滑到边界改为反弹。** 低于 800 px/s 完全不弹，手指按下立即中断。

## v1.14

修「输出数量」1 和 5 同时高亮：动态创建的 `RadioButton` 没设 id,`RadioGroup` 因此跳过了取消上一个的步骤。

## v1.13

**输出数量可选 1 / 5 / 10。** 多出来的排在「备选地址」里，整行点一下复制。备选不入历史。

要多个结果时会停用「远超目标就提前收工」，改判「合格数量够了没」。

## v1.11 – v1.12

**液态玻璃界面。** 背景四团径向渐变光斑，卡片半透明填充 + 1dp 亮描边(API 31+ 走 `RenderEffect`)。

**端口与地区改折叠，带摘要。** 收起时标题右侧显示「443 · 香港/日本」。

**补一套动效。** 统一插值器、卡片 staggerIn、折叠区高度动画靠 tag 防抖(两个 `ValueAnimator` 同时改一个 View 的 height 会让高度永久残缺)。

## v1.10

**页面从三页收敛为两页。** v1.9 拆成「扫描 / 配置 / 历史」，结果调个带宽要切页。现在参数回归首页，排在按钮和结果下面。

emoji 全部换成矢量图标。

## v1.8 – v1.9

v1.8 参数区横向滚动改 `FlowLayout` 自动换行；v1.9 加顶栏与底部导航。

## v1.7

**回源校验。** 填了节点域名时检查回源状态码，拦掉 52x 回源不通的 IP。

根因：`testRTT` 只判断响应有没有 `CF-RAY` 头，而回源失败的 521–526 同样带这个头。于是「摸到了门」被当成「门后的隧道通」，用户侧表现为连上就断。

现在用白名单：放行 101 / 2xx / 3xx / 400 / 404 / 405 / 410，判死 403 / 521–526 / 530 / 其他 5xx。分界线是「这个响应由谁生成」—— 404 由源站生成证明回源通，52x 由边缘生成证明回源断。用白名单而非黑名单，是因为上游新增错误码时白名单最坏少给一个候选，黑名单最坏又放行一个坏的。

## v1.6

**地区侦察。** 选了地区时先探子网的落地机房，再拿命中的去优选。

判定是三态的：只有拿到明确否定证据才排除，探不通归 unknown 进候补池。随机拼 IP 命中率约 1/3，把「探不通」当「不符合」会每 3 个好子网冤枉 2 个。

## v1.5

一次性修掉五个影响「优选到底选了什么」的问题：

- **测速形同虚设。** 默认期望带宽 1 Mbps 几乎任何 IP 都达标，「遇首个达标即返回」让结果等于「延迟最低」而非「最快」。现在测完 top N 选最快
- **峰值当成速度。** 原来用 `maxSpeed`(TCP 慢启动后的突发峰值)，改成 EWMA 平均
- **测速不查状态码。** 错误页的字节数被当成下载内容。现在只接受 200/206
- **单次 TCP 失败就判死。** 加丢包率：探 4 次，超 50% 判死，排序时丢包率优先于延迟
- **RTT 与测速目标域名不一致。** 实测有 IP 对前者返 301 通过、对后者返 403 不可用

另外两个：`SplitN` 缺 `else` 导致 URL 拼成 `https:///`；数据文件没有时效判断，下载过一次就永不更新。

## v1.4

优选逻辑重写。

## v1.2

端口与地区筛选。端口最要紧：CF 边缘不只监听 80/443,v1.1 固定测 80/443，如果你的节点跑在 2053，验证的和实际用的根本不是同一个端口。

同版本补 MIT LICENSE、数据来源声明、免责声明。

## v1.1

首个发布版本。基于一份早期实现重写，修掉一批结构性问题：

- **扫描永不结束。** 两层 `for{}` 都没有轮次上限，带宽填 900 就等于永久扫描。现在 `maxScanRounds = 10`
- **达不到目标就一无所获。** 原版只在达标时返回，实测 61 Mbps 本来可用却什么都拿不到
- **测速统计漏掉最后一个窗口。** 读到 `err` 直接 break，下载在 1 秒内结束时速度报 0
- **取消后仍在跑。** 并发信号量满载时阻塞，点了取消还要等已排队的测完
- **取消被显示成「未找到」。** 现在有 `cancelled` 字段
- **IPv6 前缀长度被忽略。** 固定保留前 48 位，上游一旦给出 `/64` 就会拼出不属于 CF 的地址
- **每轮重复抽样。** 每轮重新洗牌取前 100 个，轮与轮之间没有记忆。现在一次扫描内洗牌一次、游标推进
- **测速端口和 RTT 不一致。** 测速固定连 80/443，测出来的速度对应的不是要用的那个端口
- **取消后更新数据静默失败。** 取消置了 context 而 `UpdateData` 不重置，旧文件已删新文件没下，进度却显示「完成」
- **点了开始立刻点取消，取消会丢。** 「派发」和「真正开始」之间有窗口，现在加 `BeginTask()` 在派发前先建好上下文
- **取消提示写死「扫描」。** 数据更新走同一个通道，改成中性的「正在取消...」
- **JDK 路径硬编码。** 换机器直接构建失败，现在走 `JAVA_HOME`
- **夜间模式缺 danger 色。** 深红放深灰背景上对比度不足，取消和删除按钮看不清
