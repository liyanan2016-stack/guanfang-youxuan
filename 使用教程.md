# 使用教程

从 Cloudflare 官方 IP 段里挑出**你当前网络下最快的那个 IP**。结果拿去填代理配置、Hosts 或 CDN 回源。

工具本身不做代理、不加速，只申请 `INTERNET` 一个权限。结果和网络环境绑定，换 WiFi、换运营商都可能变 —— 用时再扫。

## 安装

**Android**（最低 7.0）：[Releases](../../releases) 下 APK。不确定架构就下 `universal`。

**Windows / Linux**：下可执行文件双击，自动开浏览器。**关掉黑色控制台窗口才算退出**，只关浏览器标签服务还在跑。只监听 `127.0.0.1`。

## 第一次用

1. **点顶栏「更新数据」** —— 首次必做，几秒钟，之后一个月一次
2. **期望带宽填 `1`** —— 先看实测多少再往上调
3. **端口选你节点实际用的那个** —— 最容易错的一项
4. **点「开始扫描」** —— 中途反复提示「RTT 丢包」是正常的
5. **点地址栏复制** —— 完整的 `IP:端口`，直接用

## 参数

只有前三项常用，其余默认即可。

| 参数 | 说明 |
|---|---|
| **IP 协议** | 不确定选 IPv4。IPv6 要求你的网络真支持 |
| **期望带宽** | 填 1 最快出结果；20~50 务实；超过本地宽带上限没意义 |
| **端口** | **选你节点实际用的那个。** 选错会「工具说可用、接节点报错」 |
| 输出数量 | 1 / 5 / 10。选多个会给「备选地址」，备选不入历史 |
| 连接验证 TLS | 开着就行。它决定端口列表是 TLS 组还是明文组 |
| 落地地区 | 常用地区点选，其他填 `DE,NL` 这样的代码。**筛得越窄越慢** |
| 指定 IP 段 | 留空扫官方全部。填了只扫你给的段，几分钟降到几十秒 |
| 高级 → 节点域名 | 留空用 `cloudflare.com`。填自己域名更贴近实际，填错会全测不通 |
| 高级 → 测速源 | 默认自动。「优选快、实际慢」就靠它 |
| 高级 → 测速时长 | 默认 5 秒。数字偏高时改 10 或 15 |

### 指定 IP 段怎么填

一行一条，或逗号/空格分隔，可混写：

```
104.16.0.0/13                 CIDR，自动切成 2048 个 /24
104.17.168.20                 单个 IP
104.16.0.0-104.16.3.255       起止范围（IPv6 请用 CIDR）
2606:4700::/48                IPv6
# 注释会被忽略
```

边打边校验。上限 20000 个 `/24`，别一次填 `/8`。填了就不读官方列表，所以必须真是 Cloudflare 的段。

### 测速源选哪个

速度数字是拿某个域名的文件测出来的，而运营商对不同域名的限速差别很大。

| 选项 | 说明 |
|---|---|
| 自动选择 | 探测运营商后自动挑。**推荐** |
| Cloudflare | 官方端点，通用稳定 |
| CM提供 | 社区端点，移动线路通常更接近实际 |
| 移动专属 | 适合移动宽带 / 流量卡 |
| 手动输入 | 填**你自己域名下 100MB 以上的文件**，最准 |

## 结果怎么看

**✅ 达标** —— 拿去用。

**⚠️ 有结果但没达标** —— 附「未达到 200 Mbps，返回最佳结果 61 Mbps」。**这个 IP 可用**，只是没到你设的目标。

**❌ 没找到** —— 两种说法要分清：

- 「6534 个子网已全部测过，没有一个能连通」→ 你的网络到 CF 整体不通
- 「已测试 300 个子网（共 10 轮）」→ 只是轮次用完了，**再点一次扫描**会从没测过的继续

**🚫 已取消** —— 明确显示，不会伪装成「未找到」。

## 常见问题

**一直提示「RTT 丢包」？** 正常。候选 IP 是从子网随机拼的，一个 `/24` 有 256 个地址而 CF 只在少数几个上提供服务。数据源性质决定的，不是 bug。指定 IP 段可以绕开。

**扫描慢？** 主要看期望带宽。填 1 通常十几秒，填 100 可能几分钟。上限 10 轮，最坏也会结束。

**选了地区之后慢很多？** 官方 IP 段不含地理信息，只能测完从 `CF-RAY` 反查。随机拼的 IP 大部分是死的，能连通的里面再筛地区，命中率是乘法关系。偶尔国家列为空是 CF 新机房数据还没更新，这种情况放行而不是丢掉。

**优选出来很快，实际用起来慢？** 测速源换「移动专属」或填自己域名的大文件，测速时长改 10 或 15 秒。默认 5 秒会落在运营商的「突发不限速」窗口里。

**客户端报 `closed pipe`？** 先检查端口。工具只验证「这个 IP 在**指定端口**上是活的 CF 节点」，你的节点跑在 2053 而工具测的是 443，握手能过、数据一发就被掐断。

端口对了还报，可能是 WebSocket/gRPC 路径配错、机房限制长连接、或你的反代本身没在这个 IP 上正常回源 —— 这些工具测不出来。

**扫出来的 IP 过几天不快了？** 正常，需要时重扫。

**更新数据失败？** App 会说缺哪几个文件，重试通常就好。数据来自第三方接口 `baipiao.eu.org`，接口挂了只能等。

**怎么看版本号？** 顶栏标题右边小字，点一下弹「关于」，里面有「使用说明」和「更新日志」。

**主题怎么切？** 顶栏右上圆形图标，「跟随系统 → 浅色 → 深色」轮换，会记住。

**能同时装「官方优选」和「反代优选」吗？** 可以，包名不同。

## 给开发者

```bash
./build-apk.sh              # Android debug
./build-apk.sh release      # Android release，需签名配置
./build-desktop.sh          # 桌面版，交叉编译
```

出包前会跑 `gofmt` / `go vet` / `go test`，任一失败即中止。

核心逻辑在 `scan.go`，对外 API 在 `better.go`（gomobile 入口，桌面版也调它）。Android 界面在 `android/`，桌面版在 `cmd/guanfang/`。

发版要同步改五处，漏一处就会界面版本和实际版本不一致：

1. `android/app/build.gradle` 的 `versionCode` / `versionName`
2. `better.go` 的 `libVersion`
3. `android/app/src/main/res/values/strings.xml` 的 `changelog_body`
4. `cmd/guanfang/web/index.html` 的 `DOC_CHANGELOG`
5. `更新日志.md` 与 `README.md` 下载段的文件名
