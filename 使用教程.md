# 使用教程

## 这是什么

从 Cloudflare 官方公布的 IP 段里挑出**你当前网络下最快的那个 IP**。挑出来的 IP 拿去填代理配置、Hosts 或 CDN 回源，用途你自己定。工具本身不做代理、不加速，只申请 `INTERNET` 一个权限。

**结果和网络环境绑定**：换 WiFi、换运营商、过几小时都可能变。用时再扫，别指望扫一次用一年。

名字里的「官方」指数据源是 Cloudflare 官方公布的 IP 段，与 Cloudflare 公司无关。

## 安装

**Android**（最低 7.0）：到 [Releases](../../releases) 下 APK。不确定手机架构就下 `universal`；2016 年后的手机可以下 `arm64-v8a` 省一半空间。会提示「来自未知来源」，选继续安装。

**Windows / Linux**：下对应可执行文件双击，会自动开浏览器。Windows 的 SmartScreen 警告点「更多信息 → 仍要运行」。**关掉黑色控制台窗口才算退出**，只关浏览器标签服务还在跑。只监听 `127.0.0.1`，局域网访问不到。

## 五分钟跑完第一次

**1. 点顶栏「更新数据」** —— 首次必做，没有子网列表扫不了。几秒钟。之后一个月更新一次够了。

**2. 期望带宽填 `1`** —— 先跑一次看「实测带宽」是多少，再往上调。填超过你家宽带上限永远达不到，只会白等满 10 轮。

**3. 端口选你节点实际用的那个** —— 这项最容易错，后果见下面的 closed pipe。

**4. 点「开始扫描」** —— 过程中会反复提示「这批 IP 都存在 RTT 丢包」，这是正常的，不是故障。

**5. 看结果** —— 地址栏点一下就复制，那是完整的 `IP:端口`，直接拿去配节点。

## 参数

只有前三项常用，其余保持默认即可。

| 参数 | 说明 |
|---|---|
| **IP 协议** | 不确定选 IPv4。选 IPv6 的前提是你的网络真支持，否则全连不上白扫一场 |
| **期望带宽** | 目标速度（Mbps）。填 1 最快出结果；20~50 比较务实；填超过本地宽带上限没有意义 |
| **端口** | **选你节点实际用的那个。** CF 边缘还监听 8080/2052/2053/8443 等，选错会「工具说可用、接节点报错」 |
| 输出数量 | 1 / 5 / 10，默认 1。选多个会在结果下方给「备选地址」，第一个不好用就换下一个。备选不入历史 |
| 连接验证 TLS | 开着就行。它还决定端口列表是 TLS 组还是明文组，切换时已选端口会重置 |
| 落地地区 | 可选，常用地区点选，其他填 cca2 代码如 `DE,NL`。**筛得越窄越慢**，原因见下 |
| 指定 IP 段 | 折叠区内，留空即扫官方全部。填了只扫你给的段，几分钟降到几十秒 |
| 高级 → 节点域名 | 留空用 `cloudflare.com`。填你自己的域名更贴近实际，但填错会全部测不通 |
| 高级 → 测速源 | 默认「自动选择」。「优选快、实际慢」就靠它，见下 |
| 高级 → 测速时长 | 默认 5 秒。数字偏高时改 10 或 15 |

### 指定 IP 段

一行一条，或逗号/空格分隔，可混写：

```
104.16.0.0/13                 CIDR，自动切成 2048 个 /24
104.17.168.20                 单个 IP
104.16.0.0-104.16.3.255       起止范围（IPv6 请用 CIDR）
2606:4700::/48                IPv6
# 注释会被忽略
```

边打边校验，下方实时显示识别了多少段或哪一条填错。上限 20000 个 `/24`，别一次填 `/8`。

要注意：填了就不读官方列表，所以必须真是 Cloudflare 的段；填了 v4 段却在扫 IPv6 会提示你切协议；指定段时会测完全部子网才收手（官方列表模式测到 30% 覆盖率就够）。

### 测速源

速度数字是拿某个域名的文件测出来的，那条链路和你自己的节点未必有关系，而运营商对不同域名的限速差别很大。

| 选项 | 说明 |
|---|---|
| 自动选择 | 探测运营商后自动挑，移动线路换用更友好的源。**推荐** |
| Cloudflare | 官方端点，通用稳定 |
| CM提供 | 社区端点，移动线路通常更接近实际 |
| 移动专属 | 适合移动宽带/流量卡 |
| 手动输入 | 填**你自己域名下 100MB 以上的文件**，测的是你真正会用的链路，最准。必须带文件路径，填错会直接报错不会偷偷用默认地址 |

## 结果的四种情况

**✅ 达标** —— 正常显示，没有额外说明。拿去用。

**⚠️ 有结果但没达标** —— 附一句「未达到 200 Mbps，返回最佳结果 61 Mbps」。**这个 IP 是可用的**，只是没到你设的目标。直接用，或把期望带宽调低重扫。

**❌ 没找到** —— 两种说法要分清：
- 「6534 个子网已全部测过，没有一个能连通」→ 你的网络到 Cloudflare 整体不通，检查网络或换 IP 协议
- 「已测试 300 个子网（共 10 轮），未找到可用 IP」→ 只是 10 轮额度用完了，还有大量子网没测，**再点一次扫描**会从没测过的继续

**🚫 已取消** —— 明确显示「扫描已取消」，不会伪装成「未找到」。

## 常见问题

**一直提示「这批 IP 都存在 RTT 丢包」？**
正常。候选 IP 是从子网随机拼的，一个 `/24` 有 256 个地址而 CF 只在少数几个上提供服务，扫描就是在不停试错。数据源性质决定的，不是 bug。

**扫描慢？**
主要看期望带宽。填 1 通常十几秒，填 100 可能几分钟。上限 10 轮，最坏情况也会结束。

**选了地区之后慢很多？**
官方 IP 段不含地理信息，落地机房还取决于运营商线路，只能**测完**才能从 `CF-RAY` 反查国家。随机拼的 IP 大部分是死的，能连通的里面再筛地区，命中率是乘法关系。慢不等于卡住——看进度里的秒数和轮次。偶尔国家列是空的，是 CF 新机房的位置数据还没更新，这种情况选择放行而不是丢掉。

**优选出来很快，实际用起来慢？**
高级选项里把测速源换成「移动专属」或填自己域名的大文件，测速时长改 10 或 15 秒。默认 5 秒会落在运营商的「突发不限速」窗口里，数字偏高。

**客户端报 `io: read/write on closed pipe`？**
先检查端口选对了没，这是最常见原因。工具只验证「这个 IP 在**指定端口**上是活的 CF 节点」，你的节点跑在 2053 而工具测的是 443，握手能过、数据一发就被掐断。

端口对了还报，可能是 WebSocket/gRPC 路径配错、机房限制长连接、或你的反代本身没在这个 IP 上正常回源——这些工具测不出来，要测得用「反代优选」那种基于已验证 `IP:PORT` 列表的工具。

**扫出来的 IP 过几天不快了？**
正常。CF 路由、运营商出口、机房负载都在变。需要时重扫。

**更新数据失败？**
App 会说缺哪几个文件，重试通常就好。数据来自第三方接口 `baipiao.eu.org`，本项目不托管，接口挂了就得等。0 字节文件（下载中断的残留）也算缺失。

**怎么看版本号？**
顶栏标题右边那个小字，点一下弹「关于」，里面还有「使用说明」和「更新日志」两个按钮。桌面版在顶栏右侧直接有三个链接。完整版本沿革见 [更新日志.md](更新日志.md)。

**主题怎么切？**
顶栏右上角圆形图标，点一下在「跟随系统 → 浅色 → 深色」之间轮换，会记住。

**滑到头会弹一下，是 bug 吗？**
不是，故意做的。没有这个反馈的话滚到边界就是硬生生一停，看着像卡住。慢慢滑到底不会弹。

**能同时装「官方优选」和「反代优选」吗？**
可以，包名不同（`com.gf.youxuan` / `com.cf.ip`），互不影响。

## 给开发者

```bash
./build-apk.sh              # Android debug 包
./build-apk.sh release      # Android release 包，需签名配置
./build-desktop.sh          # 桌面版，交叉编译
```

两个脚本出包前都会跑 `gofmt` / `go vet` / `go test`，任一失败即中止。

核心逻辑在 `scan.go`，对外 API 在 `better.go`（gomobile 绑定入口，桌面版也调它）。Android 界面在 `android/`，桌面版在 `cmd/guanfang/`。设计细节见 [README.md](README.md)。

发版时要同步改的五处（漏一处就会出现界面版本和实际版本不一致）：

1. `android/app/build.gradle` 的 `versionCode` / `versionName`
2. `better.go` 的 `libVersion`
3. `android/app/src/main/res/values/strings.xml` 的 `changelog_body`
4. `cmd/guanfang/web/index.html` 的 `DOC_CHANGELOG`
5. `更新日志.md` 与 `README.md` 下载段的文件名

安卓顶栏的版本号取自 `PackageManager`，不用手改。
