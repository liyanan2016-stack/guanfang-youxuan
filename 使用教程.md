# 使用教程

从零开始，五分钟跑完第一次优选。

---

## 一、这个 App 是干什么的

Cloudflare 在全球有很多机房，同一个域名在不同机房的访问速度差别很大。
Cloudflare 官方公布了自己所有的 IP 段（子网列表），这个 App 就是从这些
子网里随机生成候选 IP，逐个测试延迟和下载速度，帮你挑出**当前网络环境下最快的那一个**。

挑出来的 IP 拿去填到你的代理配置、Hosts 或者 CDN 回源里，用途取决于你自己的场景。
App 本身只负责「找出哪个 IP 快」，不做任何代理或加速。

App 只申请了 `INTERNET` 一个权限，没有后台服务、不会开机自启、不读你的通讯录相册。

一句重要的话：**优选结果和你的网络环境强相关。** 换 WiFi、换运营商、
换城市、甚至只是过了几小时，最快的 IP 都可能变。所以这个东西是「用时再扫」，
不是「扫一次用一年」。

另外说明：名字里的「官方」指的是**数据源用的是 Cloudflare 官方公布的 IP 段**，
不是「Cloudflare 官方出品」。本项目与 Cloudflare 公司没有任何关联。

---

## 二、界面长什么样

只有两个页面，底部导航切换：

- **优选** — 扫描按钮、结果、扫描参数都在这一页，从上到下就是使用顺序
- **历史** — 最近 10 条结果

顶栏右侧两个东西：**更新数据**按钮（首次使用必须先点，见下文），
和一个圆形的**主题切换**图标 —— 点一下在「跟随系统 → 浅色 → 深色」之间轮换，
当前模式会用 Toast 提示。

标题「官方优选」右边有个小字版本号（如 `v1.21`），**点一下弹「关于」**：里面写了
应用版本、build 号、核心库版本，还有两个按钮——**使用说明**（就是本文的精简版，
7 条最容易卡住的地方）和**更新日志**（最近几版改了什么）。报问题时先看一眼版本号。

参数区排在扫描按钮和结果**下面**，不是上面。这是有意的：跑第二轮时通常只想再点
一次扫描，不该每次都先划过一屏参数。

---

## 三、安装

### Android

到 [Releases](../../releases) 页下载 APK，点开安装。

- 最低系统版本：Android 7.0
- 手机会提示「来自未知来源」或「此类文件可能有害」，这是所有非应用商店安装包
  都会有的提示，选择「继续安装」/「仍要安装」即可
- 小米 / vivo / OPPO 等国产系统可能需要先在「设置 → 应用管理 → 权限 →
  安装未知应用」里给浏览器或文件管理器放行

装完桌面上会出现「官方优选」图标。

如果不清楚自己手机是什么架构，直接下 `universal` 版，体积大一点但一定能装。
想省空间的话，2016 年之后的手机基本都是 `arm64-v8a`。

### Windows / Linux 桌面版

下载对应的可执行文件，双击运行。程序会自动打开浏览器显示界面。

- Windows 会弹 SmartScreen 警告（因为没买代码签名证书），
  点「更多信息 → 仍要运行」
- 服务只监听 `127.0.0.1`，局域网内其他设备访问不到
- **关掉那个黑色控制台窗口就等于退出程序**，别直接关浏览器标签
- 配置和历史记录存在 `%AppData%\guanfang-youxuan\`（Linux 在 `~/.config/`）
- 顶栏右侧有版本号和**使用说明 / 更新日志 / 关于**三个链接，内容与安卓端一致

---

## 四、第一次使用

### 第 1 步：更新数据

**首次使用必须先做这一步。** 点界面上的「更新数据」按钮，等它跑完。

这一步会从网上下载 Cloudflare 官方的子网列表（`ips-v4.txt`、`ips-v6.txt`）
和机房位置表。不下载的话没有候选 IP，扫描无法开始。

正常几秒钟就好。如果失败了，App 会告诉你缺哪几个文件——通常是网络问题，重试即可。

数据不需要频繁更新，Cloudflare 的 IP 段变动不频繁，一个月更新一次足够。

### 第 2 步：设置参数

**IP 协议** — 选 IPv4 还是 IPv6。

不确定就选 IPv4。选 IPv6 的前提是**你的网络真的支持 IPv6**，否则所有候选 IP
都连不上，白扫一场。家庭宽带很多地方默认没开 IPv6。

**期望带宽** — 单位 Mbps，你希望优选出来的 IP 至少有多快。

这是整个界面最容易填错的一项，说详细点：

- 填 `1`（默认）：几乎任何能连通的 IP 都算达标，第一轮通常就结束，最快
- 填 `20`~`50`：比较务实的目标，一般几十秒到两分钟
- 填 `100` 以上：要看你本地宽带上限。如果你家宽带只有 100M，
  那填 200 永远达不到——App 会白跑满 10 轮，几分钟后返回「最佳结果」而不是你要的数字

填多少取决于你的实际带宽。不知道自己多少带宽就先填 1 跑一次，
看「实测带宽」是多少，再往上调。

**输出数量** — 一次要几个结果，可选 1 / 5 / 10。

默认 `1`，只要最快的那一个，出结果最快。选 5 或 10 会在测完之后多留几个备选，
排在结果下方的「备选地址」里。

什么时候值得选多个：优选出来的 IP 可能过一会儿就不好用了，手上多攒几个
就不用重扫。代价是要把候选测得更充分，时间变长。

注意备选**不写进历史记录**，只有最优的那条会存。所以想留着备用就当场复制走。

**连接验证 TLS** — 开着就行。

开启后会做完整的 TLS 握手，更接近真实访问情况，测出来的 IP 更可靠。
关掉会快一些，但可能选出「能 ping 通但实际访问不了」的 IP。

注意这个开关还决定端口列表：开启时是 TLS 端口组（443 等），
关闭时是明文端口组（80 等）。切换时已选的端口会自动重置。

**端口** — v1.2 新增，**这一项最需要你留意**。

Cloudflare 边缘节点不只监听 80/443，还有 8080、8880、2052、2082、2086、2095
（明文）和 2053、2083、2087、2096、8443（TLS）。

**选你节点实际使用的那个端口。** 选错了会出现一个很难排查的现象：工具说这个 IP
可用，拿去接节点却报 `io: read/write on closed pipe`。因为工具验证的是
「这个 IP 在 443 上是活的 CF 节点」，而你要用的是别的端口——握手能过，
数据一发就被掐断。

可以多选，但候选数会成倍增长，明显变慢。不确定就只留默认的标准端口。

**落地地区** — v1.2 新增，可选。

常用地区（香港/台湾/日本/韩国/新加坡/美国）直接点，其他地区在输入框里填
cca2 代码，比如 `DE,NL`。不选就是不限。

**筛得越窄会明显变慢**，原因见后面的「常见疑问」。

**指定 IP 段** — v1.20 新增，默认折叠、留空。

留空就是扫官方全部 IP 段（6500 多个 /24）。填了就**只扫你给的段**：候选池
小得多，扫描从几分钟降到几十秒，命中率也高得多 —— 前提是这些段在你的
网络下确实好用。

适用场景：别人分享的优质段、自己 Cloudflare 账户对应的段、某个特定机房
宣告的段。

支持的写法（一行一条，或用逗号/空格分隔，可以混着来）：

```
104.16.0.0/13                 CIDR，会自动切成 2048 个 /24
104.17.168.20                 单个 IP
104.16.0.0-104.16.3.255       起止范围（IPv6 请用 CIDR 写法）
2606:4700::/48                IPv6
# 这行是注释，会被忽略
```

边打边校验，下方会实时显示「已识别 2048 个 IPv4 /24 子网」或者具体是哪一条
填错了，不用等扫完才知道。收起折叠区后标题右侧也会显示「v4 2048 段」。

几个要注意的：

- **填了就不会读官方列表**，所以填的段必须真是 Cloudflare 的段。填了别人家
  的 IP 只会全部测不通。
- 填了 IPv4 段却在扫 IPv6（或反过来）会直接提示你切换协议，不会让你对着
  空结果猜。
- 指定段时会**测完全部子网**才收手（官方列表模式下测到 30% 覆盖率就够了），
  因为你给的池子本来就是精挑的，跳过大部分没有意义。
- 上限 20000 个 /24。超了会截断并告诉你，别一次填 `/8`。
- 可以和落地地区、测速源叠加使用。

结果里会显示「数据源」，标明这次用的是你指定的段还是官方列表。

**高级选项 → 自定义 SNI / Host** — 默认折叠，一般不用管。

留空用 `cloudflare.com`。填你自己节点的域名能让测试更贴近实际使用，
但**填错会导致全部测不通**，所以不确定就别填。

**高级选项 → 测速源** — v1.19 新增，默认「自动选择」。

这是「优选出来很快、实际用起来很慢」的头号原因。速度数字是拿某个域名的
文件测出来的，那条链路和你自己的节点未必有关系；而运营商（尤其中国移动的
国际出口）对不同域名的限速策略差别很大。

| 选项 | 说明 |
| --- | --- |
| 自动选择 | 先探测你的运营商，移动线路自动换用对移动更友好的源。**推荐** |
| Cloudflare | 官方测速端点，通用、稳定 |
| CM提供 | 社区测速端点，移动线路通常比官方源更接近实际速度 |
| 移动专属 | 直接回 100MB 文件，适合移动宽带/流量卡 |
| 手动输入 | 填你自己域名下的大文件，最准，见下 |

选「手动输入」才会出现地址输入框，填**你自己域名下一个 100MB 以上的文件**，
比如 `your.domain.com/files/100mb.bin`。这样测的就是「CF 边缘 → 回源到你的
服务器」这条你真正会用到的链路，数字最有参考价值。必须带文件路径，填错会
直接停下报错而不会偷偷用默认地址。

结果里会回显「测速地址」和「检测运营商」，速度数字是拿什么测的一目了然。

**高级选项 → 测速时长** — 默认 5 秒。

运营商的国际出口对新连接常有「突发不限速」窗口，前几秒飞快、之后掉一个
数量级。5 秒测速整段都落在窗口里，测出来比你能持续用到的带宽高得多。
实际使用明显比优选结果慢时，改成 10 或 15 秒，代价是扫描变慢。

### 第 3 步：开始扫描

点「开始扫描」。扫描过程中：

- 扫描按钮会变灰（防止误触重复启动）
- 下方出现「取消」按钮，随时可以中止
- 状态栏会实时显示进度

**你大概率会看到这样的提示：**

```
这批 IP 都存在 RTT 丢包，换下一批子网继续
```

**这是正常的，不是故障。** 原因见下面的「常见疑问」。

### 第 4 步：看结果

扫描完成后会显示：

| 字段 | 含义 |
|---|---|
| 地址 | 优选出来的 `IP:端口`，**点一下就复制**。直接拿这个完整地址去配节点 |
| 端口 | 实测通过的端口，就是你选的那个 |
| 期望带宽 | 你填的目标 |
| 实测带宽 | 实际测到的平均速度 |
| 峰值速度 | 测试过程中的最高瞬时速度 |
| 往返延迟 | RTT，越低越好 |
| 数据中心 | 这个 IP 属于哪个机房（三字母机场代码，如 HKG = 香港、NRT = 东京），后面括号里是国家代码 |
| 总用时 | 本次扫描耗时 |

如果「输出数量」选了 5 或 10，结果卡片下方还会出现**备选地址**列表，
标题里带个数，每行是「序号 + 地址 + 速度/延迟」，整行点一下就复制。
备选只显示这几项 —— 它是「第一个不好用就换下一个」的东西，机房和用时对这个
决策没帮助，全列出来反而要划很久。

历史记录保留最近 10 条，点地址直接复制，长按可以删除单条。只存最优那一条，
备选不入库。

---

## 五、结果的四种情况

这是理解这个 App 最关键的部分。扫描结束后你会遇到四种结果，含义完全不同：

### ✅ 达标

正常显示 IP 和各项数据，没有额外说明。实测带宽 ≥ 你填的期望带宽。拿去用就行。

### ⚠️ 有结果但没达标

显示 IP，同时附一句说明：

```
已测试 300 个子网（共 10 轮）未达到 200 Mbps，返回最佳结果 61 Mbps
```

**这个 IP 是可用的**，只是没达到你设的目标。61 Mbps 对大多数场景完全够用。
要么直接用，要么把期望带宽调低点重扫。

早期版本在这种情况下什么都不返回，明明有个能用的 IP 却让用户一无所获——
这是我修掉的问题之一。

### ❌ 没找到

两种不同的说法，区别很重要：

```
6534 个子网已全部测过，没有一个 IP 能连通（用时 180 秒）
```
→ 数据源里所有子网都试完了。通常意味着你的网络到 Cloudflare 整体不通，
检查网络本身，或者换 IPv4/IPv6 试试。

```
已测试 300 个子网（共 10 轮），未找到可用 IP（用时 120 秒）
```
→ 只是把 10 轮的额度用完了，还有大量子网没测。直接再点一次「开始扫描」，
会从没测过的子网继续。

### 🚫 已取消

你点了取消按钮。这个会明确显示「扫描已取消」，不会伪装成「未找到」。

---

## 六、常见疑问

**为什么一直提示「这批 IP 都存在 RTT 丢包」？**

因为候选 IP 是从子网**随机拼出来**的。比如 `104.16.0.0/24` 这个子网有 256 个地址，
Cloudflare 实际只在其中少数几个上提供服务，其余都是空的。随机拼中活的概率本来就低，
所以扫描就是在不停试错——测到一批全死的，换下一批继续。

这是数据源的性质决定的，不是 bug。多等一会儿。

**为什么扫描这么慢？**

主要看你填的期望带宽。填得越高，越难达标，就要试越多轮。填 1 通常十几秒，
填 100 可能要几分钟。

上限是 10 轮，最坏情况也会结束，不会无限跑下去（早期版本会，已修）。

**扫出来的 IP 过几天就不快了？**

正常。Cloudflare 的路由、你的运营商出口、机房负载都在变。需要的时候重扫一次。

**「按地区筛选」怎么用？为什么筛了之后变慢很多？**

v1.2 加上了。常用地区点选，其他地区在输入框填 cca2 代码（如 `DE,NL`）。

它和「反代优选」的地区筛选不是一回事。反代优选的节点列表自带国家标签，
可以先筛掉不要的再测；官方 IP 做不到——落地机房取决于运营商线路，
同一个 IP 在电信和联通下可能落到不同国家，事先无从得知。所以官方优选只能
**测完之后**从 `CF-RAY` 拿到机房三字码、反查国家，再决定要不要这个结果。

所以筛得越窄耗时增长越明显：随机拼的 IP 大部分是死的，能连通的里面还要
再筛地区，命中率是乘法关系。筛到没结果时提示会告诉你可以放宽重试，
那不是工具坏了。

偶尔有结果的国家列是空的，是因为 CF 上线新机房时位置数据可能还没更新。
这种情况选择放行而不是丢掉——不然数据更新前你什么都拿不到。

**优选出来的 IP 拿去用，客户端报 `io: read/write on closed pipe`？**

先检查**端口选对了没**。这是最常见的原因。

这个报错来自你的代理客户端（Xray / sing-box 之类），发生在协议握手之后，
说明数据通道被对端关掉了。优选工具在这之前就已经退出，跟它没关系。

但工具能帮你避开这个坑——前提是端口选对。工具只验证「这个 IP 在**指定端口**上
是活的 CF 节点」。v1.1 固定测 80/443，所以如果你的节点跑在 2053，
工具验证的和你实际用的根本不是同一个端口：握手能过（TLS 是通的），
数据一发就被掐断。v1.2 让你选端口正是为了这个。

端口选对了还报，那问题不在这里，可能是：

- 节点的 WebSocket / gRPC 路径配错
- 那个 IP 所在机房对长连接有限制
- 你的 Worker / 反代本身没在这个 IP 上正常回源

这些工具都测不出来——它验证的是「IP 是活的 CF 节点且下载快」，
不是「你的节点在这个 IP 上能跑通」。要后者得用「反代优选」那种
基于已验证 `IP:PORT` 列表的工具。

**取消按钮点了没反应？**

正常情况下应该立即响应。如果你是「点开始后立刻点取消」，早期版本这个取消会丢
（派发和真正开始之间有一段窗口），现在已经修好了。

**期望带宽填多少合适？**

不知道就填 1 跑一次，看实测带宽是多少，再据此调整。填超过你本地宽带上限
是没有意义的——永远达不到，只会白等满 10 轮。

**更新数据失败了怎么办？**

App 会告诉你具体缺哪几个文件。重试一次通常就好。

数据来自第三方公开接口 `baipiao.eu.org`，本项目不托管这些数据。
如果反复失败，可能是该接口暂时不可用，等一会儿再试。

注意：0 字节的文件（下载中断的残留）也算缺失，会被检出来。

**主题怎么切？会记住吗？**

顶栏右上角那个圆形图标，点一下在「跟随系统 → 浅色 → 深色」之间轮换，
选了会记住，下次打开还是这个。图标本身也会跟着变。

**界面上下滑到头会弹一下，是不是有 bug？**

不是，那是故意做的。滑到顶部或底部时内容会稍微越界再弹回去，
惯性滑得越快弹得越明显 —— 没有这个反馈的话，滚动到边界就是硬生生一停，
看着像卡住了。慢慢滑到底不会弹。

**能同时装「官方优选」和「反代优选」吗？**

可以，两个是独立项目，包名不同（`com.gf.youxuan` / `com.cf.ip`），互不影响。

**怎么知道我装的是哪个版本？改了什么？**

顶栏标题右边那个小字就是版本号，点一下弹「关于」，里面有应用版本、build 号和核心
库版本。同一个弹窗里的「更新日志」按钮列了最近几版的改动，完整历史在项目仓库的
[更新日志.md](更新日志.md)。

桌面版在顶栏右侧直接有「使用说明 / 更新日志 / 关于」三个链接。

---

## 七、给开发者

自己编译：

```bash
./build-apk.sh              # Android debug 包
./build-apk.sh release      # Android release 包，需签名配置
./build-desktop.sh          # 桌面版，交叉编译，不需要 Windows 机器
```

两个脚本出包前都会跑 `gofmt` / `go vet` / `go test`，任一失败就中止。

核心逻辑在 `scan.go`，对外 API 在 `better.go`（gomobile 绑定入口，桌面版也调它）。
Android 界面是 `android/`，桌面版是 `cmd/guanfang/`（本地 HTTP 服务 + 内嵌网页）。

更多细节见 [README.md](README.md)，版本沿革见 [更新日志.md](更新日志.md)。

发版时要同步改的地方（漏一处就会出现界面版本和实际版本不一致）：

1. `android/app/build.gradle` 的 `versionCode` / `versionName`
2. `better.go` 的 `libVersion`
3. `android/app/src/main/res/values/strings.xml` 的 `changelog_body`
4. `cmd/guanfang/web/index.html` 的 `DOC_CHANGELOG`
5. `更新日志.md` 与 `README.md` 下载段的文件名

安卓顶栏显示的版本号取自 `PackageManager`，不用手改；布局里那个 `v1.21` 只是
预览占位。
